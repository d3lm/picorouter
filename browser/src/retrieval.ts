// TF-IDF / tiny vector search — placeholder for slice 07

export function search(query: string, documents: string[]): string[] {
  console.log('retrieval.ts — placeholder for slice 07', query);
  return documents.slice(0, 1);
}
